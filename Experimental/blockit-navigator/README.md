# BlockIT Navigator — Superseded Proposal

Status:

```text
SUPERSEDED
PRODUCTION IMPLEMENTATION: mcp/gateway/navigator/
DO NOT USE AS CURRENT AUTHORITY
```

This folder preserves the original Navigator proposal only as design history.

Current Navigator behavior, contracts, routing, context projection, readiness, and continuation semantics are owned by:

```text
mcp/gateway/navigator/
mcp/gateway/index.ts
```

The production Navigator remains part of the existing four-tool Gateway. It is not a second MCP server, database, workflow engine, daemon, or authoring UI.

Do not copy rules from this historical proposal into active implementation. When proposal text conflicts with production source, production source wins.
