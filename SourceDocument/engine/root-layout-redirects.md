# Root Location Redirects (Operational)

## Why this looks minimal
Operational entry points are kept minimal, while other folders/files stay visible and in place for tooling compatibility.

## Hard anchors (do not move unless rebuilt)
- `package.json`, `bun.lock`, `tsconfig.json`
- `src/`, `build/`, `prompts/`, `docs/`, `openspec/`, `dist/`
- `.agents/`, `.codex/`, `.github/`, `.vscode/`

## Main quick links
- Main operation: `SourceDocument/engine/WORKFLOW_HUB.md`
- Active state: `SavedData/ACTIVE_PROJECT.md`
- Source docs: `SourceDocument/`
- MCP/Skills map: `SourceDocument/mcp-and-skills/README.md`

If anything is out of place, restore using these anchors, not by guessing new paths.
