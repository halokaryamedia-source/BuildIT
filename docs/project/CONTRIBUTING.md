# Contributing

Keep changes scoped to the active OpenSpec. Reuse existing tools and profiles before adding new ones. Update the single canonical authority instead of creating versioned duplicates. Runtime sessions stay under `workspace/` and are not committed by default. CI remains disabled during active Rework development.
