# Change Log

Short record of meaningful BlockIT knowledge/documentation changes. Historical entries retain the architecture/state that was true when recorded; current ownership is determined by source, `AGENTS.md`, the dashboard, Validation Report, Review Index, and `next-action.md`.

## 2026-08-11 — Pre-local Codex Handoff Alignment

- completed the bounded non-local source/contract/CI cleanup and moved active continuation to Codex + Blockbench local acceptance;
- restored `next-action.md` as a compact current-state snapshot rather than an implementation changelog;
- added one procedural owner, `operations/local-acceptance-runbook.md`, for the baseline local run, efficiency trace, failure classification, and post-run updates;
- aligned README/CONTEXT/dashboard/navigation/source/implementation/operations/foundation docs with the current task-class routing;
- updated current skill documentation from the historical six-skill stage to the present nine repository-owned packages, grouped into asset-authoring and repository-development routes rather than loaded together;
- corrected current prompt ownership to `mcp/prompts/bedrock_entity_workflow.md`;
- corrected current discovery semantics and default tool containment (`apply_texture` / `filter_by_material` not default-callable);
- reclassified the MCP Reduction & Stabilization Plan as historical/completed instead of current execution authority;
- refreshed the Review Index so old review bodies remain provenance without controlling current work;
- preserved the current pinned-SDK default surface baseline at 62 enabled tools / 72,775 response chars / 48,674 schema chars / 11,800 description chars.

## 2026-08-08 — Reference Fidelity Architecture

At that time the repository consolidated the then-current repository-wide development/modelling specialists and froze the **six-skill architecture of that stage**. Later 2026-08-10/11 authoring work added the current BlockIT orchestrator, texturing, and animation packages; see the current Skill Map rather than interpreting this historical entry as present routing.

Other work in this stage:

- retired old nested/stale skill ownership paths from current routing;
- simplified reference generation to an approved multi-view Modelling Brief without pixel calibration or heavy package machinery;
- documented the Reference Fidelity root cause: technically valid Cube placement can still produce a globally wrong model;
- adopted whole-form spatial reasoning + structural/visual observation + causal correction;
- documented the hard rebuild threshold for unrecognizable/multi-primary failure;
- separated structural evidence from visual approval.

## 2026-08-08 — Fidelity Source Hardening

Current Local source at that stage added/hardened:

- `inspect_model_bounds`;
- `capture_model_views`;
- `inspect_element`;
- `modify_cubes_batch`;
- strict `modify_cube`/`place_cube` targeting;
- no silent requested-group → root fallback;
- Group/bone preflight and pivot-transfer behavior;
- Cube pivot-only vs authored geometry rewrite semantics;
- explicit pivot requirement for new non-zero-rotation Cubes;
- explicit finite `from/to` requirement for newly placed Cubes.

These historical source milestones were followed by additional 2026-08-10/11 contract/semantics cleanup. Use the current Implementation Map and Validation Report for present behavior.

## 2026-08-08 — Root Docs / Obsidian Refresh

- refreshed `docs/README.md` and the foundation/knowledge indexes;
- reclassified source-selection/merge-map notes as historical adoption records;
- refreshed Obsidian dashboard, implementation/module/skill/source maps, glossary, review index, roadmap, task board, agent flow, maintenance flow, and decision guide;
- added the durable `decisions/reference-fidelity-loop.md` record;
- removed routing claims pointing at nonexistent `mcp/workflow/` or retired skill roots;
- changed the tracked Obsidian workspace landing page to `index.md` and cleaned stale recent-file paths.

## 2026-07-23 — Vault Creation

- created the repo-local Obsidian vault in `docs/knowledge/`;
- added dashboard/home notes, glossary, templates, module maps, and operations layer;
- connected Obsidian start-page concept to `index.md`;
- added early ownership and roadmap/task-board notes.
