# Bedrock Capability Boundary — Test Support

Status: regression fixture only. Current proof state is `docs/foundation/validation-report.md`.

Mapped concepts include Locators, Null Objects in Bedrock workflow, and Per-face `material_instance`. Current direct tools include `manage_locator` and `manage_null_object`.

Protected gaps:

- TextureMesh — MCP GAP — protected.
- Animation controllers creation/mutation — MCP GAP — protected.
- Native Bedrock bounding-box fields — MCP GAP — protected.
- Animated textures — MCP GAP — protected.
- Bone binding expression authoring — MCP GAP — protected.

Generic `nodes://{id}` observation remains Transitional / deferred.

Absence of a direct tool is not evidence that the native Bedrock capability may be removed.
