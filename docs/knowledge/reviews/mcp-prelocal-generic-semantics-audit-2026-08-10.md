# Generic Semantics — Test Support

Status: regression fixture only; historical detail is available in Git history.

Retained current decisions asserted by regression tests:

- **DEFAULT-DISABLE** the inherited generic per-face `Texture.apply()` wrapper for normal Bedrock single-texture authoring.
- Generic `nodes://{id}` observation: **DEFER — retain for now** until a direct Bedrock ownership requirement proves a better mapping.

This file is not a routing or product-policy owner.
