# Skill Activation Matrix

Secondary index only. Root `AGENTS.md` is the authority.

## Reference Preparation / Asset Authoring

Blockbench asset authoring uses `blockit-bedrock-entity-mcp` plus only the relevant specialist: `blockbench-bedrock-modelling`, `blockit-bedrock-texturing`, or `blockit-bedrock-animation`. Reference preparation uses `blockbench-reference-generator`.

## Repository / Plugin Development

Use `development-brief`, then at most one relevant owner: `mcp-server-development`, `blockbench-runtime-development`, `typescript-type-safety`, `bun-tooling`, or `blockbench-bedrock-modelling`.

Named MCP defects use the **Hot-Path Defect Index** in `implementation-map.md`.

## Local Acceptance — Only When Reactivated

Use the local acceptance runbook only when `next-action.md` explicitly reactivates it.
