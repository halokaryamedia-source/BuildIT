# 05 — Operations

Owns current development/validation state and local acceptance procedure.

```text
current-validation.md
next-action.md
local-acceptance-runbook.md
```

These files are operational state, not durable architecture authority.

AI load rule: read `next-action.md` only when continuing current repository work; read `current-validation.md` only when interpreting proof/status; read the runbook only when executing local acceptance.
