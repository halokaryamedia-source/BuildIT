# Archived Session Note

Legacy session artifacts were removed from the active repository tree during root consolidation to prevent stale state from being treated as current work. They remain recoverable from Git history. New production work belongs in `workspace/sessions/<asset>/` and is not committed by default.
