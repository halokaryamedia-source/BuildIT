# Ollama Integration

Ollama support uses the shared workflow in `engines/shared/`. Model selection and local-runtime adapters belong here. Do not duplicate stage profiles, state templates, or production rules.
