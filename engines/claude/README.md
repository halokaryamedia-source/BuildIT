# Claude Integration

Claude support uses the shared workflow in `engines/shared/`. Add only Claude-specific transport or adapter behavior here. Do not duplicate stage profiles, state templates, or production rules.
