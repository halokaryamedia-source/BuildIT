name: PRO-7 Sound Effects

on:
  push:
    branches:
      - Local
    paths:
      - ".github/workflows/pro7-sound-effects.yml"

permissions:
  contents: write

jobs:
  verify-and-finalize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Local
        uses: actions/checkout@v4
        with:
          ref: Local
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: ".bun-version"
      - name: Install dependencies
        working-directory: mcp
        run: bun install --frozen-lockfile
      - name: Apply bounded PRO-7 patch
        run: python3 .github/scripts/pro7_sound_effects.py
      - name: Regenerate prompt and API owners
        working-directory: mcp
        run: |
          bun run prompts:build
          bun run docs:build
      - name: Typecheck
        working-directory: mcp
        run: bun run typecheck
      - name: Contract tests
        working-directory: mcp
        run: bun run test
      - name: Measure default MCP surface
        working-directory: mcp
        run: bun run measure:surface
      - name: Production build
        working-directory: mcp
        run: bun run build
      - name: Generated docs freshness
        working-directory: mcp
        run: bun run docs:check
      - name: Commit retained PRO-7 state and remove helpers
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/pro7-sound-effects.yml
          git rm .github/scripts/pro7_sound_effects.py
          git add -A
          git commit -m "feat: support bounded Bedrock sound effect keyframes"
          git push origin HEAD:Local
